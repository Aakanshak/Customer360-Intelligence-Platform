# Machine-learning domain

`backend/app/services/ml.py` owns reproducible churn model comparison, artifact persistence, customer risk scoring, and lag-based revenue forecasting. Model-run metadata is stored in the database; binary artifacts are stored under `artifacts/`.
